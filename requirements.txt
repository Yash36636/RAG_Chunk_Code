# RAG System Dependencies (Production - Render compatible)

# Core API
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# Embeddings & Vector Search
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4

# LLM Provider (Groq only - no Gemini needed)
groq>=0.4.0
requests>=2.28.0

# Utilities
python-dotenv>=1.0.0
numpy>=1.24.0
tqdm>=4.65.0

# NOTE: We intentionally exclude:
# - tiktoken (requires Rust compilation, only needed for chunking)
# - google-generativeai (we use Groq, not Gemini)
